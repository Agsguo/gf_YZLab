#### 1.Jupyter notebook入门学习

基于代码区块的Python的IDE，将代码和文档划分为多个小单元格，可以逐步或者单独进行运行。

#### 主要优点：

1.学习速度比较快；

2.容易调试和bug查找;

3.可以加入mark Down说明文档；

#### 教程：

1.使用win + R 再输入cmd进入win终端环境；

#### 常用命令：

cd ## 切换目录 （当前盘）
E: ## 切换盘和目录 （不需要使用cd命令）

dir ## 列出当前目录的文件与文件夹
cls ## 清除终端屏幕
copy 源文件路径 目标路径 ## 复制文件
del 文件路径 ## 删除文件
mkdir 文件名 ## 创建文件夹
rmdir 文件夹名 /s /Q   ## /s：删除文件夹及内容 /Q：静默删除，不提示
move 源文件 目标路径  ## 移动文件或者重命名
ping 目标路径 ## 测试网络连接
ipconfig ## 查看网络配置信息

2.输入 ‘“jupyter notebook”进入，会在浏览器页面中出现一个窗口；

3.按“shift + Enter”确认运行；

4.蓝色模式下面，使用'X'和‘Z’，可以删除和恢复框。（命令模式）按回车可以进入绿色模式（编辑模式）

4.按左上角“Esc”退出编辑，回到命令模式；

5.按住"M"键，进入markdown模式，例如输入：# 这是一号标题。按”shfit + Enter“就会变成一号标题格式。

6.选中一个代码块，按”b"键可以在下方创建一个代码块，“a”是在上方可以创建一个代码块，“X”是剪切代码块，“D”是删除代码块，按“shift + v可以粘贴代码块（到上面），v就是粘贴，；

## 安装库文件

## 使用Jupyter notebook进行库安装
!pip install nibabel
